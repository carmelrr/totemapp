specVersion: "v1"
serviceId: "totemapp"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "totemapp-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
